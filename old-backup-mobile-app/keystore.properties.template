# Copy this file to keystore.properties and fill in real values.
# keystore.properties is gitignored — NEVER commit it.
# Store passwords in Bitwarden / 1Password.

storeFile=../souigat-release.jks
storePassword=<STRONG_PASSWORD>
keyAlias=souigat
keyPassword=<STRONG_PASSWORD>
