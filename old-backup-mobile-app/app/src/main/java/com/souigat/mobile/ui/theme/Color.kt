package com.souigat.mobile.ui.theme

import androidx.compose.ui.graphics.Color

// === Primary Brand ===
val Primary = Color(0xFF2563EB)          // Blue 600 — primary actions
val PrimaryContainer = Color(0xFFDBEAFE) // Blue 100 — selected states
val OnPrimary = Color(0xFFFFFFFF)
val OnPrimaryContainer = Color(0xFF1E40AF)

// === Semantic ===
val Success = Color(0xFF10B981)          // Emerald — synced, revenue
val SuccessContainer = Color(0xFFD1FAE5)
val Warning = Color(0xFFF59E0B)          // Amber — quarantine, stale
val WarningContainer = Color(0xFFFEF3C7)
val ErrorRed = Color(0xFFEF4444)         // Red — expenses, errors (named to avoid clash with M3 error)
val ErrorContainer = Color(0xFFFEE2E2)

// === Light Surface ===
val SurfaceLight = Color(0xFFFFFFFF)
val BackgroundLight = Color(0xFFF3F4F6)
val OutlineLight = Color(0xFFD1D5DB)
val OnSurfaceLight = Color(0xFF111827)
val OnSurfaceVariantLight = Color(0xFF6B7280)

// === Dark Surface ===
val SurfaceDark = Color(0xFF1F2937)
val BackgroundDark = Color(0xFF111827)
val OutlineDark = Color(0xFF374151)
val OnSurfaceDark = Color(0xFFF9FAFB)
val OnSurfaceVariantDark = Color(0xFF9CA3AF)
